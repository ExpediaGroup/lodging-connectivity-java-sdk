mutation SandboxCancelReservation($input: CancelReservationInput!) {
    cancelReservation(input: $input) {
        clientMutationId
        reservation {
            id
            status
        }
    }
}
