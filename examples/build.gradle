repositories {
    maven {
        url "https://oss.sonatype.org/content/repositories/snapshots/"
    }
}

dependencies {
    implementation 'com.expediagroup:lodging-connectivity-sdk:0.0.4-SNAPSHOT'
//    implementation(project(":code"))
}

